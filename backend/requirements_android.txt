# ── GeoMate Android Backend Dependencies ──
# 与 android_app/app/src/main/python/requirements.txt 保持一致。
# Chaquopy 只能安装 wheel（不能安装 sdist）：
#   - pydantic 2.x 的 Rust 核心 pydantic-core 无 Android wheel → 降级 pydantic 1.10 + fastapi 0.99
#   - SQLAlchemy 使用同步 API（不依赖 greenlet，greenlet 无 Android wheel）
#   - 不引入 torch / sentence-transformers / chromadb / pymupdf（原生依赖 Android 不兼容）

fastapi==0.99.1
uvicorn
python-multipart
sqlalchemy
pydantic==1.10.17
email-validator

# 向量检索：纯 Python（SQLite + numpy 余弦），替代 ChromaDB
numpy

# Embeddings：Android 使用纯 numpy 的 light 后端（字符 bigram 特征哈希）

# PDF：纯 Python 解析（Android 无 PyMuPDF wheel 时的兜底）
pypdf

# Utils
python-dotenv
httpx

# AI LLM（Anthropic Claude，可选；无 key 时走离线规则引擎）
anthropic
